Dziennik iteracji służy do szybkich notatek dotyczących iteracji tworzonych przez agenta AI.
Nie ma to być obszerny raport a szybka informacja co zostało zrobione w danej iteracji oraz ich numeracja.
Jeśli w danej iteracji zostało wykonane User Story należy o tym wspomnieć.

========================================
ITERACJA 01 - Data: 15.04.2026
========================================
User Story: 1. Porządek w kodzie
Status: TEST (oczekuje na manualne testy)

Wykonane prace:
- Przepisano i uporządkowano css/style.css z obszerną dokumentacją
- Przepisano i uporządkowano js/script.js z komentarzami JSDoc
- Uporządkowano index.html z komentarzami HTML
- Dodano pełne wsparcie dla <tfoot> (stopki tabeli) w całym projekcie
- Wypełniono domyślne wartości neutralne we wszystkich kontrolkach
- Dodano trzeci wiersz do przykładowej tabeli w tbody
- Stworzono szczegółowy raport w documentation/Reports/Iteracja_01_Raport.md

Zmiany w kodzie:
- style.css: dodano variables i style dla tfoot, dodano komentarze opisujące każdą sekcję
- script.js: dodano sekcję Footer z kontrolkami, dodano obsługę tfoot w Generate CSS i Load CSS
- index.html: dodano <tfoot> do przykładowej tabeli, poprawiono strukturę HTML

Następne kroki:
- Testy manualne aplikacji
- Poprawki ewentualnych bugów
- Implementacja wypełniania neutralnych wartości w wygenerowanym CSS (obecnie funkcja css() pomija puste wartości)

========================================
ITERACJA 02 - Data: 19.04.2026
========================================
User Story: 2. Brak neutralnych wartości w wygenerowanym CSS
Status: TEST (oczekuje na manualne testy)

Wykonane prace:
- Dodano nowe User Stories (2-6) wynikające z pierwszej iteracji
- Zmodyfikowano funkcję css() aby obsługiwała domyślne wartości neutralne
- Zaktualizowano funkcję getTableCSS() aby wypełniała wszystkie właściwości CSS domyślnymi wartościami neutralnymi

Zmiany w kodzie:
- js/script.js: funkcja css() teraz przyjmuje trzeci parametr defaultVal
- js/script.js: wszystkie wywołania css() w getTableCSS() zawierają domyślne wartości neutralne
- Wartości neutralne są zgodne z assumptions.txt punkt 7, 8 i 27

Rozwiązanie problemu:
Wcześniej gdy użytkownik nie ustawił np. border, to w wygenerowanym CSS nie było border: none;
Teraz funkcja getTableCSS() ZAWSZE generuje pełen CSS z neutralnymi wartościami, nawet jeśli użytkownik ich nie zmienił.
To zapobiega przeciążeniu stylów w projekcie użytkownika.

Następne kroki:
- Testy manualne - sprawdzić czy Generate CSS generuje pełny CSS z neutralnymi wartościami
- Implementacja User Story 3 - lepsze kontrolki dla borderów i marginesów

========================================
ITERACJA 03 - Data: 21.04.2026
========================================
User Story: 3. Niewygodne inputy typu text dla złożonych wartości
Status: TEST (oczekuje na manualne testy)

Wykonane prace:
- Zamieniono wszystkie kontrolki border (Table, Header, Footer, Cells, Rows) na kompozytowe kontrolki:
  * Range input (0-10px) dla szerokości ramki
  * Dropdown select (none/solid/dashed/dotted/double) dla stylu ramki
  * Color picker dla koloru ramki
- Zamieniono kontrolkę margin na 4 osobne range inputy (Top/Right/Bottom/Left) w układzie grid 2x2
- Zamieniono kontrolkę font-family na dropdown z 10 popularnymi czcionkami
- Zamieniono kontrolkę border-collapse na dropdown (collapse/separate)

Zmiany w kodzie:
- js/script.js: Przepisano funkcję createControl() z obsługą 3 nowych typów kontrolek (border, margin, select)
- js/script.js: Dodano funkcje parseBorderValue() i parseMarginValue() do parsowania przy Load CSS
- js/script.js: Zaktualizowano mapCssToVar() i extractCells() aby parsowały złożone wartości
- js/script.js: Całkowicie przepisano updateInputs() aby obsługiwała nowe kontrolki
- css/style.css: Dodano style dla .et-control-border i .et-control-margin
- Zaktualizowano definicje kontrolek w sekcjach (sections array)

Korzyści dla użytkownika:
- Nie musi już ręcznie wpisywać "1px solid black" - wystarczy przesunąć suwaki i wybrać opcje
- Intuicyjny interfejs bez znajomości składni CSS
- Mniej błędów przy wprowadzaniu wartości
- Lepsza wizualizacja (color picker, dropdowny zamiast pól tekstowych)

Następne kroki:
- Testy manualne - sprawdzić czy nowe kontrolki działają poprawnie
- Testy Load CSS - sprawdzić czy parsowanie borderów i marginesów działa
- Implementacja User Story 4 - toggle dla header/footer